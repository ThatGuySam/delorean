import { defineConfig } from "vite";

/**
 * Vite configuration for hindsight-js package.
 *
 * @see https://vitest.dev/config/
 */
export default defineConfig({
  test: {
    globals: true,
  },
});
